import 'package:flutter/material.dart';

class DevolucionesApp extends StatelessWidget {
  const DevolucionesApp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Devoluciones"),
      ),
      body: const Center(
        child: Text("Devoluciones App"),
      ),
    );
  }
}
